<%= simple_form_for(@picture) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.association :album %>
    <%= f.association :user %>
    <%= f.input :caption %>
    <%= f.input :description %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
